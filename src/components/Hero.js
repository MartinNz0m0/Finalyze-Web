const Hero = ({ text }) => {
    return (
        <header className="bg-warning text-secondary text-center">
            <>❗This app is still in beta testing❗</>
        </header>
    )
}

export default Hero;